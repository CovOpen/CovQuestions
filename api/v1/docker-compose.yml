version: "3"
services:
  nginx:
    build:
      context: ./
      dockerfile: Dockerfile
    ports:
      - 80:80
    # volumes:
    #   - ./nginx/nginx.conf /etc/nginx/nginx.conf
    #   - ./nginx/conf.d /etc/nginx/conf.d
    restart: always
    # stdin_open: true
    # tty: true
